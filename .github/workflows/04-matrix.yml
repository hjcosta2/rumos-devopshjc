jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest]
        node: [18, 20]
    steps:
      - uses: actions/checkout@v4
      # Passo que só corre no Windows
      - name: Configuração específica do Windows
        if: matrix.os == 'windows-latest'
        run: echo "A executar comandos na PowerShell..."
